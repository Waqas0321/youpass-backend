# Copy to admin/.env.development for local admin dev.
# Production API (default). For local backend via Vite proxy: /api/v1 (backend PORT=3003)
VITE_API_BASE_URL=https://youpass-backend-two.vercel.app/api/v1
