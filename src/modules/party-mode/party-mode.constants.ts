/** Max distance from event coordinates to allow Party Mode (km). */
export const PARTY_MODE_GEOFENCE_RADIUS_KM = 0.5;

/** Party Mode can activate this many hours before doors open. */
export const PARTY_MODE_PRE_EVENT_HOURS = 2;

/** Party Mode stays available this many hours after the event ends. */
export const PARTY_MODE_POST_EVENT_HOURS = 6;
